In artifacts/bharatscan/src/pages/Home.tsx:

1. Add a third fallback tier to IndexCard's data-resolution useMemo,
   below the existing liveQuote / futures / spot-CSV tiers: if none of
   those found data, call apiGetMarketHistory(def.fyersIndexSymbol, "1D",
   <7 days ago>, <today ISO>) from @/lib/api, take the last bar as the
   close, and the second-to-last bar's close as the reference for % change.
   Label this tier sourceLabel: "Last Close". Skip this tier entirely for
   index defs with no fyersIndexSymbol (matches existing guard pattern).
   Fetch these once on mount per visible index def (not on every render),
   similar to the existing liveIndexQuotes poll effect — a plain
   useEffect + useState keyed by def.key is fine, no need to poll this one
   on the 10s interval.

2. Add a market-status poll: call apiGetSchedulerStatus() from @/lib/api
   on mount and every 30s (separate effect from the existing 10s live-quote
   poll), store { marketOpenNow, connected } from the liveFeed field.

3. On each IndexCard, render a small status dot next to the existing
   sourceLabel text:
   - green (bg-success, or existing success color token) + pulsing when
     marketOpenNow && connected — i.e. genuinely live
   - orange/amber (text-amber-400, matching the existing pcrColor amber
     used elsewhere in this file) when NOT (marketOpenNow && connected) —
     i.e. showing last close, whether from the Live/Futures fallback data
     or the new Last Close tier
   Keep it small (a ~6px rounded dot is enough) and add a title attribute
   ("Live" / "Showing last close") for accessibility — don't add new text
   that could crowd the compact card layout.

Do not change IndexCard's existing Live/Futures/Spot logic or labels,
only add the new fallback tier after them and the status dot. Do not
touch the NSE Cash / Nifty 50 / Futures gainers-losers panels — those
already show "Invalid Date" only when latestDate is empty (no CSV/DB
equity data loaded this session), which is a separate, expected state,
not part of this fix.