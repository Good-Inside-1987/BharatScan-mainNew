Fix a scheduler bug first: in server/src/config/environment.ts, "runSchedulerInProcess" is
currently set to "isReplit", meaning scheduled jobs never run automatically on Electron/local
(only Replit gets in-process scheduling; Oracle is meant to use a separate PM2 process via
server/scheduler/standalone.js, which doesn't exist yet either). Change this to:
"runSchedulerInProcess: envLabel !== 'oracle'" — so both Replit AND local/Electron run scheduled
jobs in-process, and only real Oracle server deployments rely on the separate PM2 process.

Now build the nightly EOD + intraday sync jobs in server/src/services/scheduler.ts, using the
existing config.syncSchedule.eod ("0 16 * * 1-5", 4pm IST) and config.syncSchedule.intraday
("30 16 * * 1-5", 4:30pm IST) cron times already defined:

1. EOD JOB (4pm): pull that day's daily close (EOD) candle for every symbol in the "symbols"
   table (the full NSE universe, populated by the symbol master job) using the same
   getHistoricalBars logic already used by marketDataService.ts, resolution "1D". This applies
   the same on every environment — only the retention window differs
   (config.eodRetentionYears: 3 years on Replit, 10 years on Oracle/local) — apply that by
   deleting ohlcv_daily rows older than that window as part of this same job (or in the separate
   cleanup job below, whichever is cleaner).

2. INTRADAY JOB (4:30pm): behavior must differ by config.intradayUniverse and
   config.intradayCandleSize:
   - On Replit (intradayUniverse "fo_stocks", candle size "5min"): only backfill/top-up intraday
     candles for F&O-eligible stocks (is_fo_eligible = true in the symbols table), 5-minute
     resolution, keeping a rolling 3-month window (config.intradayRetentionMonths = 3 — delete
     anything older).
   - On Oracle/local (intradayUniverse "all_nse", candle size "1min"): backfill/top-up intraday
     candles for ALL NSE stocks in the symbols table, 1-minute resolution, keep forever
     (config.intradayRetentionMonths is null — never delete).
   Since this job runs once per day for the PREVIOUS trading day, and the live feed (built
   earlier) already captures 1-min ticks in real time for the 200 F&O stocks it's subscribed to,
   this job's real job is to (a) backfill for any NSE stocks NOT covered by the 200-symbol live
   feed cap, and (b) fill in any gaps if the live feed was down/disconnected that day. Use the
   existing backfill_progress table so already-covered ranges are skipped, same as the manual
   broker-load feature.

3. Both jobs must draw from the SAME shared daily request budget already enforced in
   marketDataService.ts (config.backfillDailyRequestBudget: 500 on Replit, 4000 on Oracle/local)
   — don't create a separate counter. If the budget runs out partway through, stop cleanly and
   log how many symbols were completed vs skipped, prioritizing EOD completion first, then
   intraday, since EOD is cheaper and higher value per request.

4. Update the existing Backfill Dashboard UI (Settings or wherever it currently shows "Requests
   used today") to also show which of these two nightly jobs last ran, when, and how many
   symbols were completed vs skipped due to budget.

TEST: manually trigger both jobs (temporary test route is fine) with the broker connected, and
confirm ohlcv_daily and ohlcv_intraday get populated respecting the correct per-environment rules
above, without exceeding the daily request budget.