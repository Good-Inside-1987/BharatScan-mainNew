Fix the layout of the Option Analysis → Option Chain → LTP View.

Issue:
The PUT side (right side) of the LTP View is not horizontally aligned with the CALL side. The right-side columns are shifted toward the right edge, making the table visually unbalanced. The last column (OI Change %) is too close to the right border, and the overall PUT section is not symmetrical with the CALL section.

Use the Greeks View as the reference.

The Greeks View already has the correct layout. I want the LTP View to use the exact same table structure, grid layout, spacing, column widths, alignment, and padding as the Greeks View.

Requirements:

• Compare the LTP View and Greeks View side by side.
• Reuse the same layout/grid implementation that Greeks View uses.
• Do NOT create a separate layout specifically for LTP View.
• The only difference between the two views should be the displayed columns, NOT the layout.
• The Strike column must remain perfectly centered.
• CALL and PUT sections must have exactly equal width.
• Every PUT column must mirror the corresponding CALL column.
• Header cells and body cells must line up perfectly.
• All column widths must remain consistent.
• The last PUT column (OI Change %) must have the same right padding that the CALL side has on the left.
• Equal outer spacing should exist on both sides of the table.
• The overall table should look perfectly symmetrical around the Strike column.

Do NOT change:
- Any calculations
- Option data
- API calls
- Sorting
- Filtering
- Row height
- Typography
- Colors
- Scrolling behavior
- Responsive behavior
- Existing functionality

If different CSS or grid definitions exist for LTP View and Greeks View, remove the duplicate LTP layout and make LTP View reuse the same base table/grid component used by Greeks View so both views always stay perfectly aligned.

Before finishing, verify:
✔ Strike column is centered.
✔ CALL and PUT sections have identical width.
✔ PUT side columns align exactly like CALL side.
✔ OI Change % column no longer touches the right edge.
✔ LTP View visually matches the Greeks View.
✔ No existing functionality is broken.

Do not make assumptions. Inspect the existing table component, CSS Grid/Flex layout, and column definitions, identify the root cause of the misalignment, fix it properly instead of applying temporary CSS offsets, and ensure both views share the same layout implementation.