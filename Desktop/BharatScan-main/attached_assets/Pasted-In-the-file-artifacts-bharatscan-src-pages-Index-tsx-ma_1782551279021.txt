In the file `artifacts/bharatscan/src/pages/Index.tsx`, make the following changes exactly as written. Do not rewrite any function, do not change any other logic, do not touch any other file.

---

CHANGE 1 — Add a new function after resolveSeriesFilter

Find this exact block:

  function resolveSeriesFilter(sel: string): string[] | undefined {
    if (sel === "ALL") return undefined;
    if (sel === "EQ") return ["EQ"];
    if (sel === "ETF") return seriesList.filter((s) => /ETF/i.test(s));
    if (sel === "BOND") {
      const bondCodes = new Set(["GB", "GS", "SG", "TB", "Y1", "F1", "FB", "MF", "N0", "N1", "N2", "N3", "N4", "N5", "N6", "N7", "N8", "N9", "NA", "NB", "NC", "ND", "NE"]);
      return seriesList.filter((s) => bondCodes.has(s) || /BOND|DEBT|GSEC|GILT/i.test(s));
    }
    return [sel];
  }

Replace it with this (the original function unchanged, plus one new function added right after it):

  function resolveSeriesFilter(sel: string): string[] | undefined {
    if (sel === "ALL") return undefined;
    if (sel === "EQ") return ["EQ"];
    if (sel === "ETF") return seriesList.filter((s) => /ETF/i.test(s));
    if (sel === "BOND") {
      const bondCodes = new Set(["GB", "GS", "SG", "TB", "Y1", "F1", "FB", "MF", "N0", "N1", "N2", "N3", "N4", "N5", "N6", "N7", "N8", "N9", "NA", "NB", "NC", "ND", "NE"]);
      return seriesList.filter((s) => bondCodes.has(s) || /BOND|DEBT|GSEC|GILT/i.test(s));
    }
    return [sel];
  }

  // BUG FIX: In options mode, filteredHistories are already fully filtered by
  // buildOptionHistories (symbol universe + expiry + side). The series field on
  // those synthetic histories is "OPT", not "EQ". Passing resolveSeriesFilter(series)
  // (which returns ["EQ"]) to runScan filters out every option history and gives
  // 0 results even when matches exist. Return undefined in options mode so runScan
  // skips the series gate entirely.
  function activeSeriesFilter(): string[] | undefined {
    if (scanMode === "options") return undefined;
    return resolveSeriesFilter(series);
  }

---

CHANGE 2 — inside runScanNow()

Find:
        const r = runScan(filteredHistories, filterItems, {
          series: resolveSeriesFilter(series),
          atDailyIdxFromEnd: asOfDate ? 0 : Math.abs(dayOffset),
          asOfDate,
          logicMode: topLogicMode,
        });

Replace with:
        const r = runScan(filteredHistories, filterItems, {
          series: activeSeriesFilter(),
          atDailyIdxFromEnd: asOfDate ? 0 : Math.abs(dayOffset),
          asOfDate,
          logicMode: topLogicMode,
        });

---

CHANGE 3 — inside runBacktestNow(), the runBacktest() call

Find:
        const r = runBacktest(filteredHistories, filterItems, backtestDays, {
          series: resolveSeriesFilter(series),
          asOfDate,
          logicMode: topLogicMode,
        });

Replace with:
        const r = runBacktest(filteredHistories, filterItems, backtestDays, {
          series: activeSeriesFilter(),
          asOfDate,
          logicMode: topLogicMode,
        });

---

CHANGE 4 — inside runBacktestNow(), the auto-select latest day runScan() call

Find:
          const scanRes = runScan(filteredHistories, filterItems, {
            series: resolveSeriesFilter(series),
            asOfDate: latest.date,
            logicMode: topLogicMode,
          });

Replace with:
          const scanRes = runScan(filteredHistories, filterItems, {
            series: activeSeriesFilter(),
            asOfDate: latest.date,
            logicMode: topLogicMode,
          });

---

CHANGE 5 — inside selectBacktestDay()

Find:
    const r = runScan(filteredHistories, filterItems, {
      series: resolveSeriesFilter(series),
      asOfDate: date,
      logicMode: topLogicMode,
    });

Replace with:
    const r = runScan(filteredHistories, filterItems, {
      series: activeSeriesFilter(),
      asOfDate: date,
      logicMode: topLogicMode,
    });

---

CHANGE 6 — inside the backtest CSV export/download onClick block

Find:
                      const seriesFilter = resolveSeriesFilter(series);
                      const indices = backtest.map((_, i) => i);

Replace with:
                      const seriesFilter = activeSeriesFilter();
                      const indices = backtest.map((_, i) => i);

---

VERIFICATION — after all 6 changes are done, search the file for the string resolveSeriesFilter(series). It must appear zero times as a direct call anywhere in the file. The only place resolveSeriesFilter(series) should exist is inside the body of the new activeSeriesFilter function itself. If you find any other occurrence, replace it with activeSeriesFilter() as well.